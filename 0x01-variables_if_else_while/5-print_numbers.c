#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints no 0-10
 *
 * Return: Always 0.
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)`
		printf("%d\n", num);

	return (0);
}
