#include "main.h"
/**
*_strlen-returns the length of a string
*@s:string that is passed into the function
*Return:returns the variable length
*/

int _strlen(char *s)
{
	int length = strlen(*s);

	return (length);
}
